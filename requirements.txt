# Core Flask
Flask==3.0.0
gunicorn==21.2.0
flask-cors==4.0.0

# Database
SQLAlchemy==2.0.23
Flask-SQLAlchemy==3.1.1
psycopg2-binary==2.9.9
alembic==1.13.0

# Redis & Caching (Sprint 1)
redis==4.6.0
hiredis==2.3.2

# Celery - Distributed Task Queue (Sprint 5)
celery[redis]==5.3.4

# Scheduler (Sprint 4)
APScheduler==3.10.4

# Authentication & Security (Sprint 2)
PyJWT==2.8.0
Flask-JWT-Extended==4.5.3
bcrypt==4.1.1
cryptography==41.0.7
Flask-Limiter==3.5.0
Flask-WTF==1.2.1

# Validation & Serialization
marshmallow==3.20.1
email-validator==2.1.0

# Dependency Injection (Sprint 0)
dependency-injector==4.41.0

# PDF rendering (Sprint 28 — invoice + booking PDFs)
# weasyprint 60.2 requires pydyf < 0.11 (0.11+ changed PDF constructor signature)
weasyprint==60.2
pydyf==0.10.0
Jinja2==3.1.3

# Payment Providers
stripe==14.3.0

# LLM providers (S97 — core LLM Connection Manager; extracted from cms-ai).
# Imported lazily by vbwd/llm/adapter.py only when a real client is built.
openai==2.41.1
anthropic==0.109.1

# HTTP Clients (Payment Gateways, External APIs)
httpx==0.25.2
# HTTP/2 support for httpx (required by the APNs client, S66)
h2==4.1.0
requests==2.31.0

# RSS/Atom feed parsing (S81 — wp_import plugin, D1: no hand-rolled XML)
feedparser==6.0.11

# WebSockets (Real-time notifications)
flask-socketio==5.3.6
python-socketio==5.10.0

# Environment & Configuration
python-dotenv==1.0.0

# Testing (Sprint 0, Sprint 10)
pytest==7.4.3
pytest-flask==1.3.0
pytest-cov==4.1.0
pytest-mock==3.12.0
pytest-asyncio==0.23.2
pytest-xdist==3.5.0
factory-boy==3.3.0
faker==21.0.0

# Load Testing (Sprint 10)
locust==2.17.0

# Code Quality
flake8==6.1.0
mypy==1.7.1
black==23.12.1
types-redis==4.6.0.20240106
types-python-dateutil==2.8.19.20240106
types-requests==2.31.0.20240106

# Monitoring (Celery)
flower==2.0.1

# meinchat-plus (S28.3b) — CBOR envelope grammar for E2E message validation.
# The server parses the envelope header only; it never decodes ciphertext.
cbor2==5.6.2
